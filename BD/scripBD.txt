USE bxulzddn1o1bbj5xmg2jA

CREATE TABLE CHAT(
ID INT AUTO_INCREMENT PRIMARY KEY NOT NULL,
 DESCRIPCION VARCHAR (100)  NOT NULL 
);


CREATE TABLE MEDIO_PAGO (
ID_MEDIO_PAGO INT PRIMARY KEY NOT NULL,
MEDIO VARCHAR (30) NOT NULL 
);

CREATE TABLE DOCTOR (
DOCUMENTO NUMERIC PRIMARY KEY NOT NULL,
TITULO VARCHAR (30) NOT NULL,
AÑOS_EXPERIENCIA VARCHAR (20) NOT NULL
);


CREATE TABLE DUENO_PACIENTE(
IDENTIFICACION NUMERIC PRIMARY KEY NOT NULL,
NOMBRE VARCHAR (30) NOT NULL,
APELLIDO VARCHAR (30) NOT NULL,
TELEFONO INT NOT NULL,
CORREO VARCHAR (50) NOT NULL,
CONTRASENA VARCHAR (20) NOT NULL,
ID_MEDIO INT NOT NULL,
FOREIGN KEY (ID_MEDIO) REFERENCES MEDIO_PAGO (ID_MEDIO_PAGO)
);



CREATE TABLE INFORMACION_MASCOTA (
INFORMACION_MASCOTA VARCHAR (255) PRIMARY KEY NOT NULL,
NOMBRE VARCHAR (20) NOT NULL,
EDAD INT NOT NULL,
ID_DUENO NUMERIC NOT NULL,
ID_DOCTOR NUMERIC NOT NULL,
FOREIGN KEY (ID_DUENO) REFERENCES DUENO_PACIENTE (IDENTIFICACION),
FOREIGN KEY (ID_DOCTOR) REFERENCES DOCTOR (DOCUMENTO)
);


CREATE TABLE CITA (
ID_CHAT INT NOT NULL,
ID_DUENO NUMERIC NOT NULL,
PRIMARY KEY (ID_CHAT,ID_DUENO),
FOREIGN KEY (ID_CHAT) REFERENCES CHAT (ID),
FOREIGN KEY (ID_DUENO) REFERENCES DUENO_PACIENTE (IDENTIFICACION)   
);
